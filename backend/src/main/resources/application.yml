spring:
  application:
    name:backend
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    # mysql 기본 설정
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${mysql.host:j10s003.p.ssafy.io:8999}/s10p21s003_dev?serverTimezone=UTC&useUniCode=yes&characterEncoding=UTF-8
    username: ${mysql.username:root}
    password: ${mysql.password:ssafy}

server:
  port: 8081
  servlet:
    context-path: /api

#쿼리문 로그
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
